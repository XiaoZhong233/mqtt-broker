version: '3.8'
services:
  mqtt-broker:
    container_name: mqtt-broker
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./log:/application/log
      - ./radar_output:/application/radar_output
      - ./radarData:/application/radarData
    ports:
      - "1883:1883"
    restart: always